import 'package:flutter/material.dart';

class GarlandCalibrationScreen extends StatelessWidget {
  const GarlandCalibrationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Калибровка')));
  }
}
