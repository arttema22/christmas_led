import 'package:flutter/material.dart';

class GarlandEffectsScreen extends StatelessWidget {
  const GarlandEffectsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Эффекты')));
  }
}
